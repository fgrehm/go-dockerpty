syntax_version: "4"
project_name: dockerpty
based_on:
  github: matthieudelaro/nutfile_go1.6
container_working_directory: /go/src/github.com/matthieudelaro/dockerpty
mount:
  main:
  - .
  - /go/src/github.com/matthieudelaro/dockerpty
# macros:
#   build-windows:
#     usage: build the project for Windows
#     actions:
#     - env GOOS=windows GOARCH=amd64 CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOGC=1000 SHLVL=2 go build -o `pwd | xargs basename`Windows
#     aliases:
#     - bw
